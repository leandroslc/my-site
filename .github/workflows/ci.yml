name: Build (CI)

on: [pull_request]

jobs:
  build:
    uses: ./.github/workflows/build.yml

  build-preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build site
        run: npm run build
