name: Build (CI)

on: [pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/build.yml
      - uses: actions/checkout@v2
      - name: Build site
        run: npm run build
